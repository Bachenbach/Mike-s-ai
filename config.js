// js/config.js
const config = {
    apiUrl: 'https://your-api-url.com',  // Change this to your API endpoint
    wsUrl: 'wss://your-ws-url.com',      // Change this to your WebSocket endpoint
    defaultLanguage: 'en',
    defaultTheme: 'light',
    maxMessageLength: 2000,
    refreshInterval: 30000, // 30 seconds
};
